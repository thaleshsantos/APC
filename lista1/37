""" Este programa lê um número em ponto flutuante e escreve uma mensagem apresentando o dobro desse número 
   em um formato específico:  "X = ?" (onde ? é o valor fornecido). """
 
i = float(input())
print("X = %f" % (i+i)) # Mude a instrução para mostrar i + i (ou 2 * i) 
