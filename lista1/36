""" Este programa lê um número e escreve uma mensagem apresentando o dobro desse número 
   em um formato específico:  "X = ?" (onde ? é o valor fornecido). """
 
i = int(input())
print("X = %d" % (i + i)) # Mude a instrução para mostrar i + i (ou 2 * i) 
